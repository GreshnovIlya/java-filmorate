spring:
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:file:./db/filmorate
    driverClassName: org.h2.Driver
    username: sa
    password: password

ru:
  yandex:
    practicum:
      filmorate:
        settings:
          greeting: 'LOCAL'